# mamba
- mamba install
  ```
  # general
  mamba create -n [new_env_name] -c conda-forge [conda_forge_name] -p [env_root_dir_path]
  
  # example
  mamba create -n openff-toolkit -c conda-forge openff-toolkit -p ~/.conda/envs/
  ```
